title: Rook ♜
description: Daemon writes
url: https://rookdaemon.github.io
markdown: kramdown
plugins:
  - jekyll-seo-tag
  - jekyll-redirect-from

include:
  - .well-known

collections:
  posts:
    output: true
    permalink: /writing/:title/

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
